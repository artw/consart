# antidote self-managed — antidote update keeps itself current
mattmc3/antidote

# zsh-defer must be first — enables kind:defer for plugins below
romkatv/zsh-defer

# completions — fpath only, no sourcing
zsh-users/zsh-completions kind:fpath path:src

# z — directory jumper
agkozak/zsh-z

# powerlevel10k theme — sync, must be ready before first prompt
romkatv/powerlevel10k

# heavy plugins — defer until after first prompt
zsh-users/zsh-autosuggestions kind:defer
zsh-users/zsh-syntax-highlighting kind:defer
