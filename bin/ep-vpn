#!/usr/bin/osascript
tell application "System Events"
	tell current location of network preferences
		set VPNservice to service "Office"
		set isConnected to connected of current configuration of VPNservice
		if isConnected then
			disconnect VPNservice
         return "Disconnected from Office"
		else
			connect VPNservice
         return "Connecting to Office..."
		end if
	end tell
end tell



